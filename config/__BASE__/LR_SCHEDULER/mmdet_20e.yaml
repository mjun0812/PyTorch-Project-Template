name: mmdet_20e
scheduler: IterScheduler
args:
  iter_scheduler:
    name: LinearLR
    scheduler: LinearLR
    args:
      start_factor: 0.001
      total_iters: 500
  epoch_scheduler:
    name: MultiStepLR
    scheduler: MultiStepLR
    args:
      milestones: [16, 19]
      gamma: 0.1
